---
import { Image } from "astro:assets";
import LandingHeroCover from "../../assets/Hero17July.png";

interface Props {
    title?: string;
    subtitle?: string;
}

const {
    title = "Hola 👋! Soy Álvaro Villena.",
    subtitle = "Lanzar un negocio digital es tomar decisiones con foco… y sostenerte mientras lo haces. Acompaño a founders que tienen una idea potente, pero se sienten estancados, a convertirla en producto real con un método probado: Shape Up.",
} = Astro.props;
---

<section
    class="relative min-h-screen flex items-center justify-center bg-primary overflow-x-hidden"
>
    <div class="relative z-30 max-w-7xl px-4 sm:px-6 lg:px-8 w-full">
        <!-- Infinite Scrolling Banner -->
        <div
            class="relative overflow-hidden mb-8 bg-white/10 border border-white/30"
        >
            <div class="scrolling-banner">
                <div class="scrolling-content">
                    <span
                        class="text-sm md:text-base text-white font-semibold px-4"
                        >AGENDA TU LLAMADA GRATUITA - 15 MINUTOS - SIN
                        COMPROMISO - CONSULTA PERSONALIZADA -</span
                    >
                    <span
                        class="text-sm md:text-base text-white font-semibold px-4"
                        >AGENDA TU LLAMADA GRATUITA - 15 MINUTOS - SIN
                        COMPROMISO - CONSULTA PERSONALIZADA -</span
                    >
                    <span
                        class="text-sm md:text-base text-white font-semibold px-4"
                        >AGENDA TU LLAMADA GRATUITA - 15 MINUTOS - SIN
                        COMPROMISO - CONSULTA PERSONALIZADA -</span
                    >
                    <span
                        class="text-sm md:text-base text-white font-semibold px-4"
                        >AGENDA TU LLAMADA GRATUITA - 15 MINUTOS - SIN
                        COMPROMISO - CONSULTA PERSONALIZADA -</span
                    >
                    <span
                        class="text-sm md:text-base text-white font-semibold px-4"
                        >AGENDA TU LLAMADA GRATUITA - 15 MINUTOS - SIN
                        COMPROMISO - CONSULTA PERSONALIZADA -</span
                    >
                    <span
                        class="text-sm md:text-base text-white font-semibold px-4"
                        >AGENDA TU LLAMADA GRATUITA - 15 MINUTOS - SIN
                        COMPROMISO - CONSULTA PERSONALIZADA -</span
                    >
                </div>
            </div>
        </div>
        <!-- Two Column Layout -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center max-w-6xl mx-auto">
            <!-- Left Column - Content -->
            <div class="text-left">
                <h1
                    class="text-white text-2xl md:text-3xl lg:text-4xl font-bold mb-4 drop-shadow-lg leading-tight"
                >
                    {title}
                </h1>
                <p
                    class="text-white text-base md:text-lg mb-6 leading-relaxed"
                >
                    {subtitle}
                </p>
                <!-- CTA Buttons -->
                <div class="flex flex-col sm:flex-row gap-4 mb-6">
                    <a
                        href="https://www.fidelidapp.cl/agendalanding/678e4df080622b7c255a9220"
                        class="w-full sm:w-auto inline-flex items-center justify-center px-6 py-3 bg-white text-primary rounded-full font-semibold text-base hover:bg-gray-100 transition-colors duration-300 shadow-lg hover:shadow-xl"
                    >
                        ▶ Agendar llamada de 15 minutos
                    </a>
                </div>
                <!-- Social Proof -->
                <div class="text-white/90 text-base mb-4">
                    ⭐ Fundador de Fidelidapp, Ewaffle y Suria · Método Shape Up
                </div>
                <div class="text-white/90 text-base mb-4">
                    · Master en Innovación y Emprendimiento· Coaching Ontológico ·
                    Coach de Innovación
                </div>
            </div>
            
            <!-- Right Column - Image -->
            <div class="flex justify-center lg:justify-end">
                <Image
                    src={LandingHeroCover}
                    alt="Álvaro Villena - Coach de Innovación"
                    width={512}
                    height={512}
                    class="rounded-2xl"
                />
            </div>
        </div>
    </div>
</section>

<style>
    .scrolling-banner {
        width: 100%;
        overflow: hidden;
        white-space: nowrap;
    }

    .scrolling-content {
        display: inline-block;
        animation: scroll-left 80s linear infinite;
        white-space: nowrap;
    }

    .scrolling-content span {
        display: inline-block;
        padding: 1rem 0;
    }

    @keyframes scroll-left {
        0% {
            transform: translateX(0%);
        }
        100% {
            transform: translateX(-200%);
        }
    }

    /* Ensure the banner spans full width */
    .scrolling-banner {
        margin-left: calc(-50vw + 50%);
        margin-right: calc(-50vw + 50%);
        width: 100vw;
    }
</style>

