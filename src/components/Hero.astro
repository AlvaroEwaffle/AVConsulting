---
import { Instagram, Linkedin } from "lucide-react";
import { Image } from 'astro:assets';
import HeroImage from '../assets/Hero.webp';
---

<!-- Hero Section -->
<section id="inicio" class="relative w-screen min-h-screen flex items-center justify-start text-left px-4 md:px-16 bg-[#f5f5f5] overflow-x-hidden">
    <div class="absolute inset-0">
        <Image 
            src={HeroImage} 
            alt="Hero Background" 
            class="w-full h-full object-cover object-right opacity-50"
        />
        <div class="absolute inset-0 bg-gradient-to-r from-[#f5f5f5] via-[#f5f5f5]/90 to-[#f5f5f5]/50"></div>
    </div>

    <div class="relative z-10 max-w-2xl">
        <!-- Título principal -->
        <h1 class="text-4xl md:text-5xl font-extrabold leading-tight text-[#2175a1]">
            Digital Founders
            <span class="block text-[#3ca6dd]">La guía práctica para lanzar tu negocio digital desde cero</span>
        </h1>

        <!-- Subtítulo -->
        <p class="mt-6 text-lg md:text-xl text-[#2175a1] opacity-90">
            Convierte tu idea en una startup real, sin perder tiempo ni dinero en el intento.
            <br class="hidden md:block"/>
            Aprende paso a paso cómo validar, lanzar y hacer crecer tu negocio digital en 8 semanas.
        </p>

        <!-- CTA Buttons -->
        <div class="mt-8 flex flex-col gap-3 max-w-[300px]">
            <a 
                href="https://pay.hotmart.com/L98924437P" 
                class="w-full text-center bg-[#2175a1] text-white font-bold py-4 px-6 rounded-lg text-lg shadow-lg hover:bg-[#3ca6dd] transition-all duration-300 hover:scale-105"
            >
                Unirme al programa 🚀
            </a>
            <a 
                href="#journey" 
                class="w-full text-center bg-white font-bold py-4 px-6 rounded-lg text-[#2175a1] border-2 border-[#2175a1] text-lg shadow-lg hover:bg-[#2175a1] hover:text-white transition-all duration-300"
            >
                Conoce el programa 🔥
            </a>
        </div>

        <!-- Social Icons -->
        <div class="mt-8 flex gap-6 items-center">
            <a href="https://instagram.com/chokovillena" target="_blank" rel="noopener noreferrer">
                <Instagram size={32} strokeWidth={1.5} className="text-[#2175a1] hover:text-[#3ca6dd] transition-colors" />
            </a>
            <a href="https://linkedin.com/in/alvarovillena" target="_blank" rel="noopener noreferrer">
                <Linkedin size={32} strokeWidth={1.5} className="text-[#2175a1] hover:text-[#3ca6dd] transition-colors" />
            </a>
        </div>
    </div>
</section>

<style>
    :root {
        overflow-x: hidden;
    }
    
    body {
        overflow-x: hidden;
        width: 100vw;
        margin: 0;
        padding: 0;
    }
</style>

