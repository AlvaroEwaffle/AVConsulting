---
interface Props {
  metrics: Array<{
    icon: string;
    number: string;
    label: string;
  }>;
}

const { metrics } = Astro.props;
---

<section class="py-16 bg-yellow-400">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <h2 class="text-3xl lg:text-4xl font-bold text-black mb-4">
        Resultados que Hablan por Sí Solos
      </h2>
      <p class="text-xl text-black/80 max-w-3xl mx-auto">
        Números que demuestran el impacto real de nuestro trabajo conjunto
      </p>
    </div>
    
    <div class="grid grid-cols-2 md:grid-cols-5 gap-8">
      {metrics.map((metric, index) => (
        <div class="text-center group">
          <div class="relative">
            <!-- Icon with background -->
            <div class="w-16 h-16 bg-white/20 rounded-full flex items-center justify-center mx-auto mb-4 group-hover:bg-white/30 transition-all duration-300 transform group-hover:scale-110">
              <span class="text-2xl">{metric.icon}</span>
            </div>
            
            <!-- Floating particles effect -->
            <div class="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-500">
              <div class="absolute top-0 left-1/2 w-1 h-1 bg-white rounded-full animate-ping"></div>
              <div class="absolute top-2 right-2 w-1 h-1 bg-white rounded-full animate-ping animation-delay-200"></div>
              <div class="absolute bottom-2 left-2 w-1 h-1 bg-white rounded-full animate-ping animation-delay-400"></div>
            </div>
          </div>
          
          <!-- Number -->
          <div class="text-4xl lg:text-5xl font-bold text-black mb-2 group-hover:scale-110 transition-transform duration-300">
            {metric.number}
          </div>
          
          <!-- Label -->
          <p class="text-sm lg:text-base text-black/80 font-medium leading-tight">
            {metric.label}
          </p>
        </div>
      ))}
    </div>
    
    <!-- Bottom CTA -->
    <div class="text-center mt-12">
      <p class="text-black/80 mb-6 text-lg">
        ¿Quieres ser parte de estas estadísticas?
      </p>
      <a 
        href="https://calendly.com/alvarovillena/llamada-exploracion"
        class="inline-flex items-center justify-center px-8 py-4 bg-black hover:bg-gray-800 text-white font-semibold rounded-xl transition-all duration-200 transform hover:scale-105 hover:shadow-lg"
      >
        Únete a Nuestros Éxitos
        <svg class="ml-2 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  @keyframes ping {
    75%, 100% {
      transform: scale(2);
      opacity: 0;
    }
  }
  
  .animate-ping {
    animation: ping 1s cubic-bezier(0, 0, 0.2, 1) infinite;
  }
  
  .animation-delay-200 {
    animation-delay: 200ms;
  }
  
  .animation-delay-400 {
    animation-delay: 400ms;
  }
</style> 